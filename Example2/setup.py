import setuptools

setuptools.setup(
    name="mypackage",
    version="0.0.1",
    author="Simone Rinco",
    description="Example of package with subpackages",
    packages=setuptools.find_packages()
)